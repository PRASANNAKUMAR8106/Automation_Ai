package com.autoflow.modules.channel.entity;

/**
 * Lifecycle status of a social account connected to AutoFlow AI.
 */
public enum ConnectedAccountStatus {
    ACTIVE,
    EXPIRED,
    REVOKED,
    ERROR,
    DISCONNECTED
}
